import { _ as _sfc_main$1 } from './Input-DVuEqpoa.mjs';
import { defineComponent, useModel, useId, ref, watch, mergeProps, unref, mergeModels, useSSRContext } from 'file:///home/sarna/netcoreops/node_modules/.pnpm/vue@3.5.34_typescript@6.0.3/node_modules/vue/index.mjs';
import { ssrRenderAttrs, ssrRenderComponent, ssrRenderAttr, ssrRenderList } from 'file:///home/sarna/netcoreops/node_modules/.pnpm/vue@3.5.34_typescript@6.0.3/node_modules/vue/server-renderer/index.mjs';

const _sfc_main = /* @__PURE__ */ defineComponent({
  __name: "AddressAutocomplete",
  __ssrInlineRender: true,
  props: {
    "modelValue": { default: "" },
    "modelModifiers": {}
  },
  emits: /* @__PURE__ */ mergeModels(["select"], ["update:modelValue"]),
  setup(__props, { emit: __emit }) {
    const model = useModel(__props, "modelValue");
    const emit = __emit;
    const listId = `address-${useId()}`;
    const suggestions = ref([]);
    watch(model, async (value) => {
      const term = value.trim();
      if (term.length < 2) {
        suggestions.value = [];
        return;
      }
      const response = await $fetch("/api/addresses/search", {
        query: { q: term }
      });
      suggestions.value = response.data;
      const selected = suggestions.value.find((item) => item.label === value);
      if (selected) {
        emit("select", selected);
      }
    });
    return (_ctx, _push, _parent, _attrs) => {
      const _component_UInput = _sfc_main$1;
      _push(`<div${ssrRenderAttrs(mergeProps({ class: "w-full" }, _attrs))}>`);
      _push(ssrRenderComponent(_component_UInput, {
        modelValue: model.value,
        "onUpdate:modelValue": ($event) => model.value = $event,
        list: listId,
        icon: "i-lucide-map-pin",
        class: "w-full",
        placeholder: "Zacznij pisać ulicę lub kod ULIC"
      }, null, _parent));
      _push(`<datalist${ssrRenderAttr("id", listId)}><!--[-->`);
      ssrRenderList(unref(suggestions), (suggestion) => {
        _push(`<option${ssrRenderAttr("value", suggestion.label)}></option>`);
      });
      _push(`<!--]--></datalist></div>`);
    };
  }
});
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("components/AddressAutocomplete.vue");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const __nuxt_component_9 = Object.assign(_sfc_main, { __name: "AddressAutocomplete" });
function formatAddress(record) {
  const street = record.street || record.serviceStreet || record.billingStreet;
  const locality = record.simcLocality || record.serviceSimcLocality || record.billingSimcLocality;
  const building = record.buildingNumber || record.serviceBuildingNumber || record.billingBuildingNumber;
  const apartmentNumber = record.serviceApartmentNumber || record.billingApartmentNumber;
  const apartment = apartmentNumber ? `/${apartmentNumber}` : "";
  return [
    street ? `${street.streetType || "ul."} ${street.name}` : "",
    building ? `${building}${apartment}` : "",
    locality?.name || ""
  ].filter(Boolean).join(", ");
}

export { __nuxt_component_9 as _, formatAddress as f };
