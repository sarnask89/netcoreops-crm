import { createError } from 'file:///home/sarna/netcoreops/node_modules/.pnpm/h3@1.15.11/node_modules/h3/dist/index.mjs';
import { eq, and, isNotNull } from 'file:///home/sarna/netcoreops/node_modules/.pnpm/drizzle-orm@0.45.2_@opentelemetry+api@1.9.1_@types+pg@8.20.0_pg@8.21.0/node_modules/drizzle-orm/index.js';
import { d as db, T as ftthOlts, ak as ftthOnuIpHosts, H as networkEquipment, al as getOrCreateOnuModelId, am as ftthOnuMacs, K as ftthOnus, an as ftthTransparentLinks, q as customerDevices, S as ftthPonPorts } from '../nitro/nitro.mjs';

function normalizeMac(macAddress) {
  return (macAddress == null ? void 0 : macAddress.trim().replaceAll("-", ":").toLowerCase()) || null;
}
function isMac(macAddress) {
  return macAddress !== null;
}
function analyzeTransparentOnu(macRows, knownMacs, managementMacs = []) {
  const managementSet = new Set(managementMacs.map(normalizeMac).filter((mac) => mac !== null));
  const knownByMac = new Map(knownMacs.map((row) => [normalizeMac(row.macAddress), row]));
  const downstreamMacs = [...new Set(macRows.filter((row) => row.vlanId !== "400").map((row) => normalizeMac(row.macAddress)).filter(isMac).filter((mac) => !managementSet.has(mac)))];
  const links = downstreamMacs.flatMap((macAddress) => {
    const known = knownByMac.get(macAddress);
    if (!known) return [];
    return [{
      macAddress,
      linkType: known.type,
      targetId: known.targetId
    }];
  });
  return {
    transparentCandidate: downstreamMacs.length > 1,
    downstreamMacs,
    links
  };
}

function normalizeFtthMac(macAddress) {
  return (macAddress == null ? void 0 : macAddress.trim().replaceAll("-", ":").replaceAll(".", ":").toLowerCase()) || null;
}
function buildFtthOnuPlan(onus, knownOnus) {
  const knownByKey = new Map(knownOnus.map((onu) => [`${onu.oltPort}:${onu.onuId}`, onu]));
  return onus.map((onu) => {
    const known = knownByKey.get(`${onu.oltPort}:${onu.onuId}`);
    return {
      action: known ? "update" : "create",
      entity: "ftthOnu",
      key: `${onu.oltPort}/${onu.onuId}`,
      label: `FTTH ONU ${onu.oltPort}/${onu.onuId}`,
      data: {
        onu,
        ftthOnuId: known == null ? void 0 : known.id,
        serialChanged: Boolean((known == null ? void 0 : known.serialNumber) && onu.serialNumber && known.serialNumber !== onu.serialNumber)
      }
    };
  });
}
function buildFtthIpHostPlan(ipHosts, knownOnus) {
  const knownByKey = new Map(knownOnus.map((onu) => [`${onu.oltPort}:${onu.onuId}`, onu]));
  return ipHosts.map((host) => {
    const onu = knownByKey.get(`${host.oltPort}:${host.onuId}`);
    return {
      action: onu ? "update" : "skip",
      entity: "ftthOnuIpHost",
      key: `${host.oltPort}/${host.onuId}/${host.hostId}`,
      label: `IP-host ${host.oltPort}/${host.onuId}/${host.hostId}`,
      reason: onu ? void 0 : "ONU nie istnieje jeszcze w modelu FTTH",
      data: {
        host,
        ftthOnuId: onu == null ? void 0 : onu.id
      }
    };
  });
}
function buildFtthMacMapPlan(macRows, knownOnus, knownMacs, managementMacs = [], managementVlanId = "400", oltInventoryId) {
  const knownByKey = new Map(knownOnus.map((onu) => [`${onu.oltPort}:${onu.onuId}`, onu]));
  const rowsByOnu = /* @__PURE__ */ new Map();
  const actions = [];
  for (const row of macRows) {
    if (!row.oltPort || !row.onuId) {
      actions.push({
        action: "skip",
        entity: "ftthOnuMac",
        key: normalizeFtthMac(row.macAddress) || row.macAddress,
        label: row.macAddress,
        reason: "Wpis MAC nie ma portu OLT i ONU ID",
        data: { row }
      });
      continue;
    }
    const key = `${row.oltPort}:${row.onuId}`;
    const onu = knownByKey.get(key);
    if (!onu) {
      actions.push({
        action: "skip",
        entity: "ftthOnuMac",
        key: normalizeFtthMac(row.macAddress) || row.macAddress,
        label: `${row.macAddress} / ${row.oltPort}/${row.onuId}`,
        reason: "ONU nie istnieje jeszcze w modelu FTTH",
        data: { row }
      });
      continue;
    }
    rowsByOnu.set(key, [...rowsByOnu.get(key) || [], row]);
    actions.push({
      action: "update",
      entity: "ftthOnuMac",
      key: normalizeFtthMac(row.macAddress) || row.macAddress,
      label: `${row.macAddress} / VLAN ${row.vlanId || "brak"} / ONU ${row.oltPort}/${row.onuId}`,
      data: {
        row,
        ftthOnuId: onu.id,
        managementVlan: row.vlanId === managementVlanId
      }
    });
  }
  for (const [key, rows] of rowsByOnu) {
    const onu = knownByKey.get(key);
    if (!onu) continue;
    const analysis = analyzeTransparentOnu(rows.map((row) => ({
      macAddress: row.macAddress,
      vlanId: row.vlanId || ""
    })), knownMacs, managementMacs);
    if (analysis.transparentCandidate) {
      actions.push({
        action: "update",
        entity: "ftthOnu",
        key,
        label: `ONU ${key.replace(":", "/")} jako transparent bridge`,
        data: {
          ftthOnuId: onu.id,
          downstreamMacs: analysis.downstreamMacs
        }
      });
      if (oltInventoryId) {
        actions.push({
          action: "update",
          entity: "networkEquipment",
          key: `${oltInventoryId}-ONU-${onu.oltPort}-${onu.onuId}`,
          label: `ONU ${key.replace(":", "/")} w Sie\u0107->Urz\u0105dzenia`,
          data: {
            ftthOnuId: onu.id,
            inventoryId: `${oltInventoryId}-ONU-${onu.oltPort}-${onu.onuId}`,
            onuPort: onu.oltPort,
            onuId: onu.onuId,
            downstreamMacs: analysis.downstreamMacs
          }
        });
      }
    }
    for (const link of analysis.links) {
      actions.push({
        action: "link",
        entity: "ftthTransparentLink",
        key: `${onu.id}:${link.macAddress}`,
        label: `${link.macAddress} za ONU ${key.replace(":", "/")}`,
        data: {
          ftthOnuId: onu.id,
          link
        }
      });
    }
  }
  return actions;
}

async function loadKnownFtthOnus(equipmentId, options = {}) {
  const olt = await db.query.ftthOlts.findFirst({
    where: eq(ftthOlts.networkEquipmentId, equipmentId),
    with: {
      ponPorts: {
        with: {
          onus: true
        }
      }
    }
  });
  const onus = (olt == null ? void 0 : olt.ponPorts.flatMap((port) => port.onus.map((onu) => ({
    id: onu.id,
    oltPort: port.portCode,
    onuId: onu.onuIdentifier,
    serialNumber: onu.serialNumber,
    status: onu.status
  })))) || [];
  if (!options.activeOnly) return onus;
  return onus.filter((onu) => {
    var _a;
    return ((_a = onu.status) == null ? void 0 : _a.toLowerCase()) === "active";
  });
}
async function ensureFtthOlt(equipmentId) {
  const equipment = await db.query.networkEquipment.findFirst({ where: eq(networkEquipment.id, equipmentId) });
  if (!equipment) throw createError({ statusCode: 404, statusMessage: "OLT nie istnieje" });
  const [created] = await db.insert(ftthOlts).values({
    networkEquipmentId: equipmentId,
    vendor: "Dasan",
    model: equipment.hostname || equipment.inventoryId,
    managementVlanId: 400,
    description: `Auto-utworzone na podstawie ${equipment.inventoryId}.`
  }).onConflictDoNothing().returning();
  const olt = created || await db.query.ftthOlts.findFirst({ where: eq(ftthOlts.networkEquipmentId, equipmentId) });
  if (!olt) throw createError({ statusCode: 500, statusMessage: "Nie mo\u017Cna utworzy\u0107 profilu FTTH OLT" });
  return { olt, equipment };
}
async function ensurePonPort(oltId, portCode) {
  const [created] = await db.insert(ftthPonPorts).values({
    oltId,
    portCode,
    label: `PON ${portCode}`,
    status: "ACTIVE"
  }).onConflictDoNothing().returning();
  const port = created || await db.query.ftthPonPorts.findFirst({
    where: and(eq(ftthPonPorts.oltId, oltId), eq(ftthPonPorts.portCode, portCode))
  });
  if (!port) throw createError({ statusCode: 500, statusMessage: `Nie mo\u017Cna utworzy\u0107 portu PON ${portCode}` });
  return port;
}
async function applyOnu(oltEquipmentId, onu) {
  const { olt } = await ensureFtthOlt(oltEquipmentId);
  const ponPort = await ensurePonPort(olt.id, onu.oltPort);
  const existing = await db.query.ftthOnus.findFirst({
    where: and(eq(ftthOnus.ponPortId, ponPort.id), eq(ftthOnus.onuIdentifier, onu.onuId))
  });
  const existingBySerial = onu.serialNumber ? await db.query.ftthOnus.findFirst({
    where: eq(ftthOnus.serialNumber, onu.serialNumber)
  }) : null;
  const values = {
    networkEquipmentId: null,
    serialNumber: onu.serialNumber || null,
    status: onu.status || "UNKNOWN",
    signalRx: onu.signalRx || null,
    lastSeenAt: /* @__PURE__ */ new Date()
  };
  if (existing) {
    await db.update(ftthOnus).set(values).where(eq(ftthOnus.id, existing.id));
    return existing.id;
  }
  if (existingBySerial) {
    await db.update(ftthOnus).set({
      ponPortId: ponPort.id,
      onuIdentifier: onu.onuId,
      ...values
    }).where(eq(ftthOnus.id, existingBySerial.id));
    return existingBySerial.id;
  }
  const [created] = await db.insert(ftthOnus).values({
    ponPortId: ponPort.id,
    onuIdentifier: onu.onuId,
    ...values
  }).returning();
  return created == null ? void 0 : created.id;
}
async function syncDasanOnusToFtth(equipmentId, onus, mode) {
  const knownBefore = await loadKnownFtthOnus(equipmentId);
  const actions = buildFtthOnuPlan(onus, knownBefore);
  if (mode !== "apply") return actions;
  for (const onu of onus) {
    await applyOnu(equipmentId, onu);
  }
  return actions;
}
async function syncDasanOnuIpHostsToFtth(equipmentId, ipHosts, mode) {
  const knownOnus = await loadKnownFtthOnus(equipmentId);
  const knownByKey = new Map(knownOnus.map((onu) => [`${onu.oltPort}:${onu.onuId}`, onu]));
  const actions = buildFtthIpHostPlan(ipHosts, knownOnus);
  if (mode !== "apply") return actions;
  for (const host of ipHosts) {
    const onu = knownByKey.get(`${host.oltPort}:${host.onuId}`);
    if (!onu) continue;
    const values = {
      ipOption: host.ipOption || null,
      macAddress: normalizeFtthMac(host.macAddress),
      currentIp: host.currentIp || null,
      currentMask: host.currentMask || null,
      currentGateway: host.currentGateway || null,
      primaryDns: host.primaryDns || null,
      secondaryDns: host.secondaryDns || null,
      hostName: host.hostName || null,
      lastSeenAt: /* @__PURE__ */ new Date()
    };
    const existing = await db.query.ftthOnuIpHosts.findFirst({
      where: and(eq(ftthOnuIpHosts.onuId, onu.id), eq(ftthOnuIpHosts.hostId, host.hostId))
    });
    if (existing) {
      await db.update(ftthOnuIpHosts).set(values).where(eq(ftthOnuIpHosts.id, existing.id));
    } else {
      await db.insert(ftthOnuIpHosts).values({
        onuId: onu.id,
        hostId: host.hostId,
        ...values
      });
    }
  }
  return actions;
}
async function loadKnownMacs() {
  const [devices, equipment] = await Promise.all([
    db.query.customerDevices.findMany({ where: isNotNull(customerDevices.macAddress) }),
    db.query.networkEquipment.findMany({ where: isNotNull(networkEquipment.macAddress) })
  ]);
  return [
    ...devices.flatMap((device) => device.macAddress ? [{
      macAddress: device.macAddress,
      type: "CUSTOMER_DEVICE_BEHIND_ONU",
      targetId: device.id
    }] : []),
    ...equipment.flatMap((row) => row.macAddress ? [{
      macAddress: row.macAddress,
      type: "BACKBONE_BEHIND_ONU",
      targetId: row.id
    }] : [])
  ];
}
async function loadManagementMacs(equipmentId) {
  const knownOnus = await loadKnownFtthOnus(equipmentId);
  const managementMacs = [];
  for (const onu of knownOnus) {
    const hosts = await db.query.ftthOnuIpHosts.findMany({ where: eq(ftthOnuIpHosts.onuId, onu.id) });
    managementMacs.push(...hosts.flatMap((host) => host.macAddress ? [host.macAddress] : []));
  }
  return managementMacs;
}
async function linkCustomerDeviceToTransparentOnu(ftthOnuId, customerDeviceId) {
  const onu = await db.query.ftthOnus.findFirst({ where: eq(ftthOnus.id, ftthOnuId) });
  if (!onu) return;
  await db.update(customerDevices).set({
    ftthOnuId,
    onuEquipmentId: onu.networkEquipmentId || null
  }).where(eq(customerDevices.id, customerDeviceId));
}
async function syncDasanMacMapToFtth(equipmentId, rows, mode) {
  const equipment = await db.query.networkEquipment.findFirst({ where: eq(networkEquipment.id, equipmentId) });
  if (!equipment) throw createError({ statusCode: 404, statusMessage: "OLT nie istnieje" });
  const knownOnus = await loadKnownFtthOnus(equipmentId);
  const knownByKey = new Map(knownOnus.map((onu) => [`${onu.oltPort}:${onu.onuId}`, onu]));
  const knownById = new Map(knownOnus.map((onu) => [onu.id, onu]));
  const knownMacs = await loadKnownMacs();
  const managementMacs = await loadManagementMacs(equipmentId);
  const actions = buildFtthMacMapPlan(rows, knownOnus, knownMacs, managementMacs, "400", equipment.inventoryId);
  if (mode !== "apply") return actions;
  const modelId = await getOrCreateOnuModelId();
  for (const row of rows) {
    const macAddress = normalizeFtthMac(row.macAddress);
    const onu = row.oltPort && row.onuId ? knownByKey.get(`${row.oltPort}:${row.onuId}`) : void 0;
    if (!macAddress || !onu) continue;
    const values = {
      vlanId: row.vlanId ? Number.parseInt(row.vlanId, 10) : null,
      gemId: row.gemId || null,
      sourceCommand: "show olt mac",
      status: row.status || "dynamic",
      lastSeenAt: /* @__PURE__ */ new Date()
    };
    const existing = await db.query.ftthOnuMacs.findFirst({
      where: and(eq(ftthOnuMacs.onuId, onu.id), eq(ftthOnuMacs.macAddress, macAddress))
    });
    if (existing) {
      await db.update(ftthOnuMacs).set(values).where(eq(ftthOnuMacs.id, existing.id));
    } else {
      await db.insert(ftthOnuMacs).values({
        onuId: onu.id,
        macAddress,
        ...values
      });
    }
  }
  for (const action of actions) {
    if (action.entity === "ftthOnu" && action.action === "update" && typeof action.data.ftthOnuId === "string") {
      const ftthOnuId2 = action.data.ftthOnuId;
      await db.update(ftthOnus).set({ transparentCandidate: true }).where(eq(ftthOnus.id, ftthOnuId2));
      const knownOnu = knownById.get(ftthOnuId2);
      if (knownOnu) {
        const inventoryId = `${equipment.inventoryId}-ONU-${knownOnu.oltPort}-${knownOnu.onuId}`;
        const equipmentValues = {
          inventoryId,
          modelId,
          parentEquipmentId: equipmentId,
          hostname: inventoryId.toLowerCase(),
          serialNumber: knownOnu.serialNumber || null,
          equipmentRole: "CLIENT_PE",
          bridgeMode: true,
          onuPort: knownOnu.oltPort,
          onuId: knownOnu.onuId,
          isOnline: true,
          status: "IN_USE",
          notes: `ONU zweryfikowana jako transparent bridge w OLT ${equipment.inventoryId}.`
        };
        const existingEquipment = await db.query.networkEquipment.findFirst({ where: eq(networkEquipment.inventoryId, inventoryId) });
        const storedEquipment = existingEquipment ? await db.update(networkEquipment).set(equipmentValues).where(eq(networkEquipment.id, existingEquipment.id)).returning().then((rows2) => rows2[0] || existingEquipment) : await db.insert(networkEquipment).values(equipmentValues).returning().then((rows2) => rows2[0] || null);
        if (storedEquipment) {
          await db.update(ftthOnus).set({ networkEquipmentId: storedEquipment.id }).where(eq(ftthOnus.id, ftthOnuId2));
        }
      }
      continue;
    }
    if (action.entity !== "ftthTransparentLink" || action.action !== "link") continue;
    const link = action.data.link;
    const ftthOnuId = typeof action.data.ftthOnuId === "string" ? action.data.ftthOnuId : null;
    if (!ftthOnuId || !link || typeof link !== "object") continue;
    const typedLink = link;
    const macAddress = normalizeFtthMac(typedLink.macAddress);
    if (!macAddress || !typedLink.linkType || !typedLink.targetId) continue;
    const values = {
      linkType: typedLink.linkType,
      customerDeviceId: typedLink.linkType === "CUSTOMER_DEVICE_BEHIND_ONU" ? typedLink.targetId : null,
      backboneEquipmentId: typedLink.linkType === "BACKBONE_BEHIND_ONU" ? typedLink.targetId : null,
      confidence: 100,
      lastSeenAt: /* @__PURE__ */ new Date()
    };
    const existing = await db.query.ftthTransparentLinks.findFirst({
      where: and(eq(ftthTransparentLinks.onuId, ftthOnuId), eq(ftthTransparentLinks.macAddress, macAddress))
    });
    if (existing) {
      await db.update(ftthTransparentLinks).set(values).where(eq(ftthTransparentLinks.id, existing.id));
    } else {
      await db.insert(ftthTransparentLinks).values({
        onuId: ftthOnuId,
        macAddress,
        ...values
      });
    }
    if (typedLink.linkType === "CUSTOMER_DEVICE_BEHIND_ONU") {
      await linkCustomerDeviceToTransparentOnu(ftthOnuId, typedLink.targetId);
    }
  }
  return actions;
}

export { syncDasanMacMapToFtth as a, syncDasanOnusToFtth as b, loadKnownFtthOnus as l, syncDasanOnuIpHostsToFtth as s };
