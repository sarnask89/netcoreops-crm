import { Selection } from 'd3-selection';
import { GeoProjection } from 'd3-geo';
import { TopoJSONMapPoint } from '../types';
import { TopoJSONMapConfigInterface } from '../config';
export declare function updateSelectionRing<AreaDatum, PointDatum, LinkDatum>(pointSelectionRing: Selection<SVGGElement, unknown, null, undefined>, selectedPoint: TopoJSONMapPoint<PointDatum> | null, pointData: TopoJSONMapPoint<PointDatum>[], config: TopoJSONMapConfigInterface<AreaDatum, PointDatum, LinkDatum>, projection: GeoProjection, currentZoomLevel: number): void;
