function renderBackground(backgroundRect, bleedLeft, bleedTop, onClick) {
    backgroundRect
        .attr('width', '100%')
        .attr('height', '100%')
        .attr('transform', `translate(${-(bleedLeft !== null && bleedLeft !== void 0 ? bleedLeft : 0)}, ${-(bleedTop !== null && bleedTop !== void 0 ? bleedTop : 0)})`)
        .style('cursor', 'default')
        .on('click', () => {
        onClick();
    });
}

export { renderBackground };
//# sourceMappingURL=background.js.map
